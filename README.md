How to RUN :
ensure docker is running locally
update the Assemby AI Api key in the .env in apps/server
run the setup.sh file in the root directory to setup
once the config is done , use ``` npm run dev ``` command to spawn the server localy